"""Raw line sources: files, SSH + cmdline (README.md "input"), with more
backends addable under ingestion/backends/ without changing anything
downstream -- every backend just yields raw lines into envelope splitting.
"""
